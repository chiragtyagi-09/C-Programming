#include <stdio.h>

int main()
{
	int i,num,num1;

    printf("\nEnter from where to start -> ");
    scanf("%d",&num);

	printf("\nEnter when to stop -> ");
	scanf("%d",&num1);

	for (int i=num;i<=num1;i++)
	{
       printf("\n%d",i);
	}

return 0;	
}
