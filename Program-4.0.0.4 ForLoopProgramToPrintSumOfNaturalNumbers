#include <stdio.h>


int main()
{
    int i,n, sum=0;

    printf("\nEnter a positive number : ");
    scanf("%d",&n);

    for( i=1; i<=n; ++i)
    {
      sum += i;
    }

    printf("\nSum = %d",sum);

return 0; 
}
