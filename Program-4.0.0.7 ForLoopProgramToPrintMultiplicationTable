#include <stdio.h>

int main()
{
    int i,n,num;
    printf("\nEnter the number you want multiplication table of : ");
    scanf("%d",&num);

    for( i=1; i<=10; ++i)
    {
    printf("\n%d * %d = %d\n",num,i,num*i);
    }
    
return 0; 
}
